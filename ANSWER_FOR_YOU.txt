You found another bug! 

The reason you got "ERR_CONNECTION_REFUSED" is because the app actually crashed this time! When it tried to generate the permanent security certificate, it tried to save it to the `monitoring_data` folder, but because we just rebooted a fresh install, that folder didn't exist yet, so it crashed!

I have just fixed the code to ensure it creates the folder first before generating the certificate.

HOW TO RECONNECT AND FIX IT:
I noticed in your terminal that your last SSH command failed. That's because you accidentally typed it backwards! 
You typed `ssh pi1@policam.local`, but it should be:
ssh policam1@pi1.local

Once you SSH back in, run the installer one more time to pull this final bug fix:
curl -sSL https://raw.githubusercontent.com/jakubstenc/YOLO_camera/master/install.sh | bash

After it finishes, the app will instantly boot up and the `https://pi1.local:5000` page will load!
